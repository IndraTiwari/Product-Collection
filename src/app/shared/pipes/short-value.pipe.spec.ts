import { ShortValuePipe } from './short-value.pipe';

describe('ShortValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortValuePipe();
    expect(pipe).toBeTruthy();
  });
});
